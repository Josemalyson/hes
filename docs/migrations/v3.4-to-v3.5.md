# Migration Guide: HES v3.4.0 → v3.5.0

## Breaking Changes

### Novos campos em current.json (RULE-26)
```bash
python3 -c "
import json
with open('.hes/state/current.json') as f: s = json.load(f)
s.setdefault('step_budget', {'DISCOVERY':{'max':15,'used':0},'SPEC':{'max':20,'used':0},'DESIGN':{'max':20,'used':0},'DATA':{'max':15,'used':0},'RED':{'max':25,'used':0},'GREEN':{'max':30,'used':0},'SECURITY':{'max':10,'used':0},'REVIEW':{'max':15,'used':0}})
s.setdefault('token_tracking', {'tokens_estimated':0,'cost_usd_estimated':0.0})
s.setdefault('model', None)
with open('.hes/state/current.json', 'w') as f: json.dump(s, f, indent=2)
print('Migration OK')
"
```

## Novos diretórios
```bash
mkdir -p .hes/schemas .hes/models .hes/evals/tasks .hes/evals/baselines \
         .hes/context/tool-outputs scripts/ci .github/workflows
```

## Novos arquivos a copiar
```bash
cp skills/11-eval.md skills/12-harness-tests.md ./skills/
cp skills/reference/step-budget-protocol.md \
   skills/reference/handoff-schemas.md \
   skills/reference/context-engineering.md \
   skills/reference/telemetry-protocol.md \
   skills/reference/skill-versioning.md ./skills/reference/
cp scripts/hooks/step-budget.sh scripts/hooks/context-offload.sh \
   scripts/hooks/telemetry.sh ./scripts/hooks/
chmod +x ./scripts/hooks/*.sh
```
